# rag-service 运行时依赖（与 pyproject.toml [project.dependencies] 同步）
# 安装：pip install -r requirements.txt
# Python >= 3.11

# ---- Web 框架 ----
fastapi>=0.115.0
uvicorn[standard]>=0.30.0           # ASGI 服务器
python-multipart>=0.0.9             # /parse /ingest multipart 文件上传
jinja2>=3.1.0                       # UI 模板（/ui/*）

# ---- 配置 / 数据模型 ----
pydantic>=2.7.0
pydantic-settings>=2.4.0            # .env 加载

# ---- Qdrant 向量库 ----
qdrant-client>=1.11.0

# ---- PDF 解析 ----
PyMuPDF>=1.24.0                     # PyMuPDF 启发式版面分析（离线 fallback）
pytesseract>=0.3.10                 # Tesseract OCR 兜底（图片型 PDF）
pillow>=10.0.0                      # pytesseract 图像依赖

# ---- 中文分词 / BM25 ----
jieba>=0.42.1                       # 中文 sparse 检索分词
rank-bm25>=0.2.2                    # BM25 备用实现（Qdrant sparse 不可用时降级）

# ---- Embedding（在线 HTTP 调用，无需本地模型） ----
httpx[socks]>=0.27.0                # Google gemini-embedding-001 / MinerU / Jina API（含 SOCKS 代理支持）

# ---- Reranker（默认 flashrank：ONNX 本地，18-120MB） ----
onnxruntime>=1.17.0                 # flashrank CPU 推理后端
flashrank>=0.1.5                    # 轻量 ONNX Cross-Encoder
# 若 RERANKER_PROVIDER=local 启用 BAAI/bge-reranker-v2-m3，需额外：
sentence-transformers>=3.0.0        # 本地 CrossEncoder（PyTorch 重，568MB 模型）

# ---- 通用工具 ----
loguru>=0.7.2                       # 日志
numpy>=1.26.0                       # 向量数值计算
